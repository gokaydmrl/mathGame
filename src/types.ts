export type NumInAr = number[];
export interface Stats {
  status: string;
  trueOnes: number;
  falseOnes: number;
}
export type inputChanging = React.ChangeEvent<HTMLInputElement>;
export type strInAr = string[]